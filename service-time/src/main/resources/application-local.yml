server:
  port: 8010

info:
  tags:
    environment: 'local'
    service: 'rest'

opentracing:
  jaeger:
    udp-sender:
      host: 'localhost'
      port: 6831

audit:
  flume:
    name: "agent007"
    enabled: true
    properties:
      channel.type: memory
      channel.capacity: 200